# Both options are nightly-only rustfmt features.

# Split imports into std / external crates / crate-local blocks, sorted within each.
group_imports = "StdExternalCrate"

# Merge imports from the same crate into a single `use` tree.
imports_granularity = "Crate"
